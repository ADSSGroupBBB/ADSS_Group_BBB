package Domain_employee;
/**
 * The ShiftType enum represents the different types of work shifts in the organization.
 * Currently, the system supports two shift types: morning and evening.

 */
public enum ShiftType {
    MORNING,
    EVENING;
}